<% @entries.each do |entry| %>
  <div class="index container">
      <div class="index image"><%=link_to image_tag(entry.photo_url), entry_path(entry) %></div>
      <div class="index created_at">Created <%= time_ago_in_words(entry.created_at) %> ago</div>
  </div>
<% end %>
